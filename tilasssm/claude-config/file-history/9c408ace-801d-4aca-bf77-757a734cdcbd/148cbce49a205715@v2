<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ClazzMapper">

    <resultMap id="clazzResultMap" type="com.example.pojo.Clazz">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="room" column="room"/>
        <result property="beginDate" column="begin_date"/>
        <result property="endDate" column="end_date"/>
        <result property="studentNum" column="student_num"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 条件分页查询班级 -->
    <select id="list" resultMap="clazzResultMap">
        SELECT id, name, room, begin_date, end_date, student_num, status, create_time, update_time
        FROM clazz
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="begin != null and begin != ''">
                AND begin_date &gt;= #{begin}
            </if>
            <if test="end != null and end != ''">
                AND begin_date &lt;= #{end}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id
    </select>

    <!-- 查询全部班级 -->
    <select id="listAll" resultMap="clazzResultMap">
        SELECT id, name, room, begin_date, end_date, student_num, status, create_time, update_time
        FROM clazz
        ORDER BY id
    </select>

    <!-- 根据ID查询班级 -->
    <select id="getById" resultMap="clazzResultMap">
        SELECT id, name, room, begin_date, end_date, student_num, status, create_time, update_time
        FROM clazz
        WHERE id = #{id}
    </select>

    <!-- 新增班级 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO clazz (name, room, begin_date, end_date, status)
        VALUES (#{name}, #{room}, #{beginDate}, #{endDate}, #{status})
    </insert>

    <!-- 删除班级 -->
    <delete id="deleteById">
        DELETE FROM clazz WHERE id = #{id}
    </delete>

    <!-- 修改班级 -->
    <update id="update">
        UPDATE clazz
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="room != null and room != ''">room = #{room},</if>
            <if test="beginDate != null">begin_date = #{beginDate},</if>
            <if test="endDate != null">end_date = #{endDate},</if>
            <if test="studentNum != null">student_num = #{studentNum},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 各班级人数统计 -->
    <select id="countStudentByClazz" resultType="java.util.HashMap">
        SELECT c.name AS name, COUNT(s.id) AS value
        FROM clazz c
        LEFT JOIN student s ON c.id = s.clazz_id
        GROUP BY c.id, c.name
    </select>

</mapper>
