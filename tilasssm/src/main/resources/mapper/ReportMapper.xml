<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ReportMapper">

    <!-- 员工性别分布统计 -->
    <select id="empGenderData" resultType="java.util.HashMap">
        SELECT CASE WHEN gender = 1 THEN '男' ELSE '女' END AS name,
               COUNT(*) AS value
        FROM emp
        GROUP BY gender
    </select>

    <!-- 员工职位人数统计 -->
    <select id="empJobData" resultType="java.util.HashMap">
        SELECT CASE job
                 WHEN 1 THEN '班主任'
                 WHEN 2 THEN '讲师'
                 WHEN 3 THEN '学工主管'
                 WHEN 4 THEN '教研主管'
                 ELSE '其他'
               END AS name,
               COUNT(*) AS value
        FROM emp
        GROUP BY job
    </select>

    <!-- 学员学历分布统计 -->
    <select id="studentDegreeData" resultType="java.util.HashMap">
        SELECT CASE degree
                 WHEN 1 THEN '初中'
                 WHEN 2 THEN '高中'
                 WHEN 3 THEN '大专'
                 WHEN 4 THEN '本科'
                 WHEN 5 THEN '硕士'
                 WHEN 6 THEN '博士'
                 ELSE '其他'
               END AS name,
               COUNT(*) AS value
        FROM student
        GROUP BY degree
    </select>

    <!-- 班级人数统计 -->
    <select id="studentCountData" resultType="java.util.HashMap">
        SELECT c.name AS name, COUNT(s.id) AS value
        FROM clazz c
        LEFT JOIN student s ON c.id = s.clazz_id
        GROUP BY c.id, c.name
    </select>

</mapper>
